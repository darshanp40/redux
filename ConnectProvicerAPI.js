/* 
    knows when the store changes - use store.subscribe
    what state is needed - use mapStateToProps
    get the store from the context
*/
function connect(mapStateToProps) {
    return Component;
}
